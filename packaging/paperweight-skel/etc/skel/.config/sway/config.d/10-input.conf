# Generic laptop input defaults

input type:touchpad {
    tap enabled
    natural_scroll enabled
    dwt enabled
    middle_emulation enabled
    scroll_method two_finger
}

input type:keyboard {
    repeat_delay 500
    repeat_rate 35
}

seat seat0 xcursor_theme Adwaita 20
