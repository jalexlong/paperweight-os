# Chromebook-specific input overrides — loaded after skel's 10-input.conf
# Faster key repeat for the Chromebook keyboard feel.
# Dark cursor to match Catppuccin Macchiato.

input type:keyboard {
    repeat_delay 200
    repeat_rate 35
}

seat seat0 xcursor_theme Adwaita-dark 20
