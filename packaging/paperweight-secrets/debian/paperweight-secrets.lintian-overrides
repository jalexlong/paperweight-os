# paperweight-secrets ships its own /etc/skel fragments (gpg-agent.conf,
# bashrc.d, sway keybind), same pattern as paperweight-skel/paperweight-chromebook.
package-contains-file-in-etc-skel

# chown -R is scoped to a single existing user's bashrc.d dir during
# skel-fragment propagation only, same rationale as paperweight-skel.
recursive-privilege-change
