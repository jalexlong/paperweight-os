# Allow sudo-group users to switch the GRUB boot menu theme
# without a password. The helper only copies within /usr/share/grub/themes/paperweight/
# and runs update-grub.
%sudo ALL=(root) NOPASSWD: /usr/bin/paperweight-grub-theme
