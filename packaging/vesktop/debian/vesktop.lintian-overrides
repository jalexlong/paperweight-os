# Electron app ships in /opt/ by upstream convention — acceptable for a personal repo
dir-or-file-in-opt

# Electron bundles these libraries internally; intentional for self-contained distribution
embedded-library freetype
embedded-library lcms2
embedded-library libjpeg
embedded-library libjsoncpp
embedded-library openjpeg
embedded-library srtp
embedded-library tiff
