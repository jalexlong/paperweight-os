# Electron app ships in /opt/ by upstream convention — acceptable for a personal repo
dir-or-file-in-opt

# Electron bundles all libraries internally; intentional for self-contained distribution
embedded-library

# Personal apt repo — no Debian bug to reference in initial upload
initial-upload-closes-no-bugs
